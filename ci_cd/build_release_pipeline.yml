trigger:
  batch: true
  branches:
    include: 
    - master
    - vnext

pool:
  name: "SelfHosted Ubuntu 18"

steps:
- script: python3 build_pipelines/build_release.py
  workingDirectory: $(Build.SourcesDirectory)
  continueOnError: true