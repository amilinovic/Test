from setuptools import find_packages, setup

# Setup custom_utility
setup(
    # Name of package
    name='marlinkpy',
    packages=['marlinkpy'],
    version='0.1.1',
    description='Libraries to work with in databricks',
    author='Derrick Bakhuis, Motion10',
    license='MIT',
    # list python libraries we need to install to use custom library
    install_requires=[],
)